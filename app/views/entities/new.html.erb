<% if can? :manage, @group %>
<div>
  <%= render @group %>

<div class="edit-group-container">
</div>
   <%= render "form", establishment: @establishment %>
</div>


  <%= link_to "Back to entities", group_entities_path(@group), class:"back-button" %>
</div>
</div>
<% else %>
  "Exit"
<% end %>

